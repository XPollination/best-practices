# Gardener v0.0.2 Deliverables

read_only API parameter + SKILL.md update for discovery queries.

- Commit ccbe2af: added read_only:boolean to MemoryRequest interface in brain API
- Updated gardener SKILL.md to use read_only:true on all discovery queries

See PDSA: `../pdsa/`
